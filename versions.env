# versions.env - Central version definitions for fedora-bootc-zfs
# Edit this file to update versions across CI and local builds

# Fedora base image version
FEDORA_VERSION=43

# ZFS stable release
ZFS_STABLE_VERSION=2.3.5

# ZFS release candidate (set to empty string when no RC exists)
ZFS_RC_VERSION=2.4.0-rc5
